
import logo from '../../assets/logo/logo.png'

const Logo = () => {
    return (
        <div>
            <a class="navbar-brand" href="#">
                <img height={40} src={logo} alt=""/>
            </a>
        </div>
    );
}

export default Logo;
